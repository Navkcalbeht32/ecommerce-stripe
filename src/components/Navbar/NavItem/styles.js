import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    navItem: {
      height: 'fit-content',
      width: 'fit-content',
      padding: '10px 14px',
    },
    navItemText: {
        fontSize: '15px',
        fontWeight: '500'
    }

  }));